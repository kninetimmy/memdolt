// Package embedding provides memdolt's production tokenizer, embedding, and
// reranking engine. Open provisions every model and runtime artifact through
// the committed SHA-256 manifest before initializing ONNX Runtime.
package embedding
